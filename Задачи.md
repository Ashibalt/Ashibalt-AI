1. Если нажать продолжить, когда превышел лимит " Превышен лимит запросов. Подождите 30-60 секунд.", то провайдер может снова вернуть ошибку, но он уже не покажется, но и ответ модели не продолжится. Так же если запрос продолжается, то туллбара не должно быть под ответом модели, потому что ответ завершился но потом продолжился(только если продолжение это не новый запрос с текстом продолжить, который считаеся как новый запрос и итерации начинаются с 1/50).
2. Функция поиска текста работает не корректно, то есть модель правильно вызывает его, например пытается искать текст "ModelSelector" в одной папке где несколько файлов, но поиск показывает 0, хотя в некоторых файлах в этой папке на самом деле был искомый текст, но функция не вернула.
3. Модель всё еще может вызывать функции без ограничений по времени, например 2 функции за меньше чем одну секунду, если модель быстрая.
4. Убери Pony Alpha openrouter/pony-alpha из кода, чтобы оно не появилось по умолчанию.
5. Убрать ashibaltproxy из проекта(код и упоминания), он не нужен.
6. Либо создать новый файл Build.md, либо переделать текущие readme.md, потому что проект теперь opensource(раньше был закрытым), так же если требуется то файл docs, где подробно расписано про код. Лицензию я поменял, так же я поправил readme(то что сейчас всего 4 провайдера). Основной readme на английском, так же убедись что для vscode marketplace есть свой один readme где не будет build(если ты решишь редактирвоать все). 
7. Создай git igonre. Важно, надо придумать как нам отделить версию расширения который уйдет в marketplace и версию для git. Например надо убрать блок с донатом для гит версии и еще может быть что то(это правильно будет вообще или такое не делается обычно?). В общем если простых вариантов не будет то напиши обсудим. я авторизовался в терминале gh auth login, так что управляй гитом через терминал. Пока что мы ничего не выпускаем в общий доступ.
1. Это не задача, а предупреждение: сделай всё в одинаковой цветовой схеме, то есть цвета из варианта c настроек и шрифты, пусть будут везде. 
2. Вариант C(C:\Users\kordh\Desktop\A19801\settings.html). вернёмся к настройкам, и так сделай следующее. Пусть оно будет не как вкладка которая выдвигается, а пусть будет модальным окном. Полностью скопируй дизайн "Вариант C — Сплит-панель с боковой навигацией".
3. Блок из: чат, метрики, дашборд изменений, выглядит жирным, громоздким и не вписывается в дизайн. Нужно сделать красивый дизайн, чистый и не жирный. Постарайся сильно на этом. Пример того как я хочу чтобы он выглядел, я приложил скриншот, но не нужно копировать на 100%, сделай схожую.
4. добавь в changelog инфу о том, что так же добавлено автовыбор провайдера(опиши) и включено для всех моделей паралельный вызов tool для режима агента.  


После выполнения задач до конца, записывай его в CHANGELOG.md. Если ты исправляешь какую то проблему после внесения в changelog патча, то версия если была 0.11.0 -> 0.11.1. То есть все исправления это третье число в нумеровании. Так же, исправление считается то, если я не упомянул проблему после того как ты её решил.
Для коммитов, используй github cli, то есть gh <command> <subcommand> [flags]. а НЕ git <command>.